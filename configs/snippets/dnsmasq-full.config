# DEPRECATED — do not append in workflow config merge step
# Forcing CONFIG_PACKAGE_dnsmasq-full=y triggers Kconfig cycle on LEDE (dnsmasq_full_nftset).
# Target DEFAULT_PACKAGES already includes dnsmasq; PassWall works with stock dnsmasq.
